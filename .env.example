# NormBridge LLM 运行时配置
# 复制为 .env 并填入你的真实值，应用启动时会自动加载（dotenv，process.cwd()/.env）。
# .env 已被 .gitignore 屏蔽，不会进版本库。

# --- 必填 ---
# 任何 OpenAI 兼容服务的 API key（OpenAI / Azure / DeepSeek / 阿里通义千问 / OpenRouter / 本地 vLLM 等）
OPENAI_API_KEY=sk-...

# --- 可选 ---
# OpenAI-Compatible 接口 base URL。留空走 OpenAI 官方 https://api.openai.com/v1
# 例：
#   DeepSeek      → https://api.deepseek.com/v1
#   Azure OpenAI  → https://<your-resource>.openai.azure.com/openai/deployments/<deployment-name>
#   阿里通义千问  → https://dashscope.aliyuncs.com/compatible-mode/v1
#   OpenRouter    → https://openrouter.ai/api/v1
#   本地 vLLM     → http://127.0.0.1:8000/v1
# OPENAI_BASE_URL=

# 默认模型（同时用于 Schema 编译和搜索）
# OpenAI:   gpt-4.1-mini / gpt-4.1 / gpt-4o-mini / gpt-4o
# DeepSeek: deepseek-chat / deepseek-reasoner
# Qwen:     qwen-plus / qwen-max
NORMBRIDGE_AGENT_MODEL=gpt-4.1-mini

# 可选：分别指定 Schema 编译 / 搜索的模型（不写则用 NORMBRIDGE_AGENT_MODEL）
# 编译需要长上下文 + 结构化输出，搜索可以小模型省 token。
# NORMBRIDGE_COMPILE_MODEL=gpt-4.1
# NORMBRIDGE_SEARCH_MODEL=gpt-4.1-mini

# OpenAI API 风格：chat_completions（默认，兼容最广）/ responses（OpenAI 官方新接口）
# 第三方服务（DeepSeek / Qwen / Xiaomi MiMo / vLLM / 大部分代理）只支持 chat_completions。
# 用 OpenAI 官方且想用新接口时设为 responses。
# OPENAI_API_STYLE=chat_completions

# 单次 LLM 调用的 text-block 上限（运行时安全网）。默认 800。一般不用改——
# 真正决定每次调用大小的是下面的 BLOCKS_PER_CALL（每节会被切成多个小窗口）。
# NORMBRIDGE_MAX_BLOCKS_PER_PROMPT=800

# 编译时每次只处理前 N 个章节（验证阶段）。默认 5。想覆盖更多章节就调大。
# NORMBRIDGE_MAX_SECTIONS=5

# 每次 LLM 调用处理多少个 text-block。关键参数：模型“每个 shall/must 句子产一条
# requirement”，所以响应大小取决于窗口里有多少 modal verb，而非输入长度。窗口越小，
# 单次响应越小、越不会撞超时。默认 200（dense 规范里约 ~20 条 requirement/窗口）。
# NORMBRIDGE_BLOCKS_PER_CALL=200

# 每个章节最多切几个窗口处理（防止超大章节炸成上百次调用）。默认 4（≈ 覆盖前 800 块）。
# 验证阶段想覆盖更全、且不在乎耗时，可调大。
# NORMBRIDGE_WINDOWS_PER_SECTION=4

# 单次 LLM 请求超时（毫秒）。慢端点上一次合法响应常要 110-118s（一个窗口可能输出
# 1.5-2.8 万字符），故默认 180000（3 分钟）留足余量；端点真挂时仍会明确报错。
# NORMBRIDGE_REQUEST_TIMEOUT_MS=180000

# 客户端层重试次数（OpenAI SDK maxRetries），对所有调用立即生效。默认 0：调高会让
# 每一次慢调用都按 (超时×次数) 累加，反而更卡。一般保持 0，用下面的节级重试代替。
# NORMBRIDGE_MAX_RETRIES=0

# 节级智能重试：仅当某节的编译调用遇到“瞬时”错误（超时 / 429 / 5xx / 网络重置）时，
# 冷却后重试，且只拖慢失败的那几节（成功节零开销，schema 解析失败不重试）。
# 共享端点高负载导致的偶发超时，靠它能把丢掉的章节抢回来。默认重试 1 次。设 0 关闭。
# NORMBRIDGE_SECTION_RETRIES=1

# 节级重试的基础冷却（毫秒），每多重试一次翻倍（指数退避）。默认 20000（20 秒）。
# NORMBRIDGE_RETRY_COOLDOWN_MS=20000
